import { Header, Footer } from '@components';

const MainPage = () => {
    return (
        <div>
            <Header />
            메인페이지 테스트
            <Footer />
        </div>
    );
};

export default MainPage;
